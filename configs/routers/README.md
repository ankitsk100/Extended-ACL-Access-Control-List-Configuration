# Router Configurations

This folder contains router CLI configurations used in this lab.

## Files
- `R1-EXTENDED-ACL-IT_ONLY.txt` — Extended ACL to allow only IT PCs to access the DHCP server (10.10.10.10)
